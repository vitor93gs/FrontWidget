import { CloseButton } from "../../CloseButton";

export function Conteudo() {
	return (
		<>
			<header className="flex flex-row items-stretch">
				<span className="text-xl leading-6">QUAL SEU BUGGER</span>
				<CloseButton />
			</header>
		</>
	);
}
